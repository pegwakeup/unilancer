/**
 * Components Index
 * Central export point for all main application components
 */

// Layout Components
export { default as Navbar } from './Navbar';
export { default as Footer } from './Footer';

// Auth & Routing
export { default as PrivateRoute } from './PrivateRoute';

// Hooks
export { useScreenSize } from './hooks/use-screen-size';
export { useDebouncedDimensions } from './hooks/use-debounced-dimensions';
